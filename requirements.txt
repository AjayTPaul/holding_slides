# Informa Connect Session Slide Builder
# Install with: pip install -r requirements.txt

# Core dependencies
streamlit>=1.28.0
python-pptx>=0.6.21
Pillow>=9.0.0
pandas>=2.0.0

# Note: Standard library imports (base64, io, os, tempfile, pathlib) are included with Python
